// Hours required to pass after matching until being ghosted is acceptable
export const matchedGhostedDifferenceHours = 24;

// Hours between reporting being ghosted and being rematched
export const ghostedRematchDifferenceHours = 24;

// Regex matching the ghost warning message
export const ghostWarningMessageRegex = /^\*\*Buddy Project Ghosting\*\*$/gm;
