export const oddQuestions = "These are odd questions, look, so odd";
export const evenQuestions = "These are even questions, look, so even";
export const intro = (buddyId: string) =>
  `This is an intro, look, so intro, your buddy is <@${buddyId}> which might say invalid-user for you on mobile but das ok.`;
